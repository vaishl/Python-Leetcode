class lastwordlength:
    def lengthOfLastWord(self, s: str) -> int:
        """
        :type s: str
        :rtype: int
        """

        #meow = " ".join(s.split())
        words = s.split()
        last = words[-1]
        return len(last)

#Finding length of last word in a string. runs at O(n) complexity
